WAP insert 10 on sorted array
#include<stdio.h>
int main()
{
        int a[7]={2,3,5,7,11,13};
        int num,i,j,ele=6;
        printf("enter num :");
        scanf("%d",&num);
        for(i=ele-1 ;i>=0 && a[i]>num ;i--)
        {
                a[i+1]=a[i];
        }
        a[i+1] =num;
        for(i=0 ;i<7;i++)
                printf("%d ",a[i]);
return 0;
}
