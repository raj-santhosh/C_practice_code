WAP count all set bit in array
#include<stdio.h>
int main(){
      int a[5]={10,15,64,100,511},i;
        int b[5],
        pos,set=0;
        for(i=0 ;i<5 ;i++)
        {
                set=0;
                for(pos=31 ;pos>=0 ;pos--)
                {       if(a[i]>>pos&1)
                                set++;
                }
                b[i]=set;
                  set+=set;

        }

        for(i=0 ;i<5 ;i++)
                printf("b:%d\n",b[i]);
printf("set count :%d,clear);
return 0;
}
