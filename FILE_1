WAP scan a data from a file and print
#include<stdio.h>
int main(int argc,char**argv)
{
        char s[20];
        if(argc!=2)
        {
                printf("usage:./a.out out fname\n");
                return 1;
        }
        struct _IO_FILE *fp;
        fp=fopen(argv[1],"r");
        if(fp==0)
        {
                printf("File is not present.\n");
                return 1;
        }
        char ch;
                while((ch = fgetc(fp)) != -1)
                        printf("%c",ch);
return 0;
}
